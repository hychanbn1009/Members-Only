extends layout

block content
    p Are you sure you want to delete this Post?
        h3 #{post.title}
        p #{post.message}
        span Author: #{post.author_name}
        span Date: #{moment(post.timestamp).format('MM/DD/YYYY')}
        form(action='' method='POST')
            button(type="submit" class="btn btn-danger") Delete